<Window x:Class="NiumaLauncher.InstallInspectionDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="启动检查详情（只读）"
        Width="840"
        Height="600"
        MinWidth="680"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#101318"
        Foreground="#F1F3F5"
        FontFamily="Microsoft YaHei">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="这里只显示检查快照，可选择并复制文字；不会执行恢复。"
                   FontSize="16"
                   TextWrapping="Wrap"/>

        <TextBox x:Name="DetailsTextBox"
                 Grid.Row="1"
                 Margin="0,16,0,16"
                 Padding="12"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="#181C23"
                 Foreground="#F1F3F5"
                 BorderBrush="#2C333E"
                 FontSize="14"/>

        <Button Grid.Row="2"
                Content="关闭详情"
                HorizontalAlignment="Right"
                Padding="24,10"
                Background="#C7F36B"
                Foreground="#11151B"
                IsCancel="True"/>
    </Grid>
</Window>